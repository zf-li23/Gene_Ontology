# scrin Dataset Placeholder

Place your scrin (mRNA co-localization) interaction edges and annotation files here, then set `config.yaml -> paths.datasets.scrin`.
Expected:
- `scrin_edges.tsv`: tab-separated `gene_a gene_b [weight]`
- `scrin_go_annotations.tsv`: optional GO/annotation table `gene_id go_term description`
